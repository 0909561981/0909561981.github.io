// Enumeration
const TileType = Object.freeze({
    WALL: 'wall',
    SPIKE: 'spike',
    WELL: 'well',
    GROUND: 'ground'
});

// Enumeration(沒用到)
const ActionType = Object.freeze({
    MOVE: 'MOVE',
    ATTACK: 'ATTACK',
    IDLE: 'IDLE'
});

// Enumeration(沒用到)
const RoleType = Object.freeze({
    PLAYER: 'player',
    ENEMY: 'enemy',
    BOSS: 'boss'
});